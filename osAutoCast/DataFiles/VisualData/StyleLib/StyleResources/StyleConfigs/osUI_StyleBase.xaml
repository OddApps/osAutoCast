<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:osEffects="clr-namespace:osAutoCast.osEffectManager"
                    xmlns:osStyle="clr-namespace:osAutoCast.osStyle">

    <osEffects:TexelSizeConverter x:Key="TexelSizeConverter" />

    <Style x:Key="StyleBase_Btn" TargetType="{x:Type Button}">
        <Setter Property="Background" Value="#FF1F1F1F" />
        <Setter Property="Foreground" Value="#FF8C8A8A" />
        <Setter Property="Width" Value="257" />
        <Setter Property="MinHeight" Value="34" />
        <Setter Property="FontSize" Value="20" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Padding" Value="0,2"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="ContentTemplate" Value="{StaticResource osEffectText}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="btnBorder" Background="{TemplateBinding Background}"
                            Width="{TemplateBinding Width}" 
                            CornerRadius="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(osStyle:osStyles.CornerRadius)}"
                            BorderThickness="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(osStyle:osStyles.BorderThickness)}"
                            BorderBrush="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(osStyle:osStyles.BorderBrush)}">
                        <ContentPresenter x:Name="btnContent" Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}"
                                          VerticalAlignment="Center" HorizontalAlignment="Center" Margin="{StaticResource osBtn_ContentPadding}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" TargetName="btnBorder"
                                    Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(osStyle:osStyles.Background)}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <Style x:Key="StyleBase_Toggle" TargetType="{x:Type ToggleButton}">
        <Setter Property="Background" Value="#FF1F1F1F" />
        <Setter Property="Foreground" Value="#FF8C8A8A" />
        <Setter Property="Width" Value="257" />
        <Setter Property="MinHeight" Value="34" />
        <Setter Property="FontSize" Value="20" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Padding" Value="0,2"/>
        <Setter Property="BorderThickness" Value="0,0,0,0"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="ContentTemplate" Value="{StaticResource osEffectTextToggle}" />
        <Setter Property="Template" Value="{Binding RelativeSource={RelativeSource TemplatedParent},
            Path=(osStyle:osStyles.Template)}" />
    </Style>

    <Style x:Key="StyleBase_Border" TargetType="{x:Type Border}">
        <Setter Property="BorderBrush" Value="#A1222222" />
        <Setter Property="BorderThickness" Value="0,0,0,0" />
        <Setter Property="Margin" Value="0"/>
    </Style>

    <Style TargetType="{x:Type osStyle:osUpDownTextBox}">
        <Setter Property="BorderBrush" Value="#FF383838"/>
        <Setter Property="Background" Value="#FF575757"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="4,0,22,0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type osStyle:osUpDownTextBox}">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="2">

                        <Grid>
                            <!-- Text -->
                            <ScrollViewer
                                x:Name="PART_ContentHost" BorderThickness="0"
                                Margin="0"/>

                            <!-- Buttons -->
                            <Grid
                                Width="18"
                                HorizontalAlignment="Right" 
                                Background="#EEE">

                                <Grid.RowDefinitions>
                                    <RowDefinition/>
                                    <RowDefinition/>
                                </Grid.RowDefinitions>

                                <RepeatButton
                                    Grid.Row="0" UseLayoutRounding="True"
                                    Content="▲" Background="#FFa1a1a1" BorderThickness="0"
                                    FontSize="9" Focusable="False"
    IsTabStop="False"
                                    Command="{x:Static osStyle:osUpDownTextBoxCmds.Increase}"/>

                                <RepeatButton
                                    Grid.Row="1" UseLayoutRounding="True"
                                    Content="▼" Background="#FFa1a1a1" BorderThickness="0"
                                    FontSize="9" Focusable="False"
    IsTabStop="False"
                                    Command="{x:Static osStyle:osUpDownTextBoxCmds.Decrease}"/>
                            </Grid>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBox">
        <Setter Property="Background" Value="#FF575757"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="#555"/>
        <Setter Property="BorderThickness" Value="0"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <Border
                        x:Name="Border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="3">

                            <DockPanel>
                                <!-- Drop Arrow -->
                                <ToggleButton
                                DockPanel.Dock="Right"
                                Width="20" FontSize="14"
                                Focusable="False" 
                                IsChecked="{Binding IsDropDownOpen,
                                    RelativeSource={RelativeSource TemplatedParent},
                                    Mode=TwoWay}">
                                    ▼
                                </ToggleButton>

                                <!-- Selected Item -->
                                <ContentPresenter
                                Margin="6,2"
                                VerticalAlignment="Center"
                                Content="{TemplateBinding SelectionBoxItem}"/>
                            </DockPanel>
                        </Border>

                        <!-- Popup -->
                        <Popup
                        IsOpen="{TemplateBinding IsDropDownOpen}" 
                        Placement="Bottom" AllowsTransparency="True">
                            <Border
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1">
                                <ScrollViewer VerticalScrollBarVisibility="Hidden">
                                    <ItemsPresenter/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBoxItem">
        <Setter Property="Background" Value="#FF626262"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Padding" Value="4"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border
                    Background="{TemplateBinding Background}">
                        <ContentPresenter/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    
</ResourceDictionary>