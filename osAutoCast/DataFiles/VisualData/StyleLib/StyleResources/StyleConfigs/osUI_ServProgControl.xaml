<UserControl x:Class="osControls.osLoadSpinner" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:osAutoCast"
             x:Name="root" Width="{Binding SpinnerSize, ElementName=root}" Height="{Binding SpinnerSize, ElementName=root}"
             UseLayoutRounding="True" SnapsToDevicePixels="True">
    <UserControl.Resources>
                <Storyboard x:Key="RotateStoryboard" RepeatBehavior="Forever">

            <DoubleAnimation Storyboard.TargetName="SpinnerRotate"
                             Storyboard.TargetProperty="Angle" RepeatBehavior="Forever"
                             From="0" To="360"
                             Duration="0:0:1.0" />

           <DoubleAnimation Storyboard.TargetName="Ring" RepeatBehavior="Forever"
                             Storyboard.TargetProperty="StrokeDashOffset"
                             From="0" To="-16.8"
                             Duration="0:0:1.0" />
        </Storyboard>
    </UserControl.Resources>

    <Grid>
        <Viewbox>
            <Canvas Width="100" Height="100">
                <Ellipse x:Name="Ring"
                         Width="100" Height="100"
                         Stroke="{Binding SpinnerBrush, ElementName=root}"
                         StrokeThickness="{Binding StrokeThickness, ElementName=root}"
                         StrokeStartLineCap="Round"
                         StrokeEndLineCap="Round"
                         StrokeDashArray="4.8 12"
                         RenderTransformOrigin="0.5,0.5"
                         SnapsToDevicePixels="True"
                         UseLayoutRounding="True"
                         RenderOptions.EdgeMode="Unspecified">
                    <Ellipse.RenderTransform>
                        <RotateTransform x:Name="SpinnerRotate" Angle="0" />
                    </Ellipse.RenderTransform>
                </Ellipse>
            </Canvas>
        </Viewbox>
    </Grid>
</UserControl>
