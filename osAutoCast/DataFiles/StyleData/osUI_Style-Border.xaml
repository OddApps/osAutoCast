<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="HideShowGameMenu" TargetType="Border">
        <Setter Property="MaxHeight" Value="0" />
        <Setter Property="Height" Value="0" />
        <Setter Property="Opacity" Value="0" />
        <Setter Property="Visibility" Value="Collapsed" />
        <Setter Property="ClipToBounds" Value="True" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsChecked, ElementName=btnShowGameMenu}" Value="True">
                <Setter Property="Visibility" Value="Visible"/>
                <Setter Property="Opacity" Value="1"/>
                <Setter Property="MaxHeight" Value="36"/>
                <DataTrigger.EnterActions>
                    <BeginStoryboard x:Name="GameMenuExpand">
                        <Storyboard FillBehavior="Stop">
                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Visibility">
                                <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="{x:Static Visibility.Visible}" />
                            </ObjectAnimationUsingKeyFrames>
                            <DoubleAnimation Storyboard.TargetProperty="MaxHeight" From="0" To="36" Duration="0:0:0.50">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseInOut" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseInOut" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </DataTrigger.EnterActions>
                <DataTrigger.ExitActions>
                    <RemoveStoryboard BeginStoryboardName="GameMenuExpand" />
                </DataTrigger.ExitActions>
            </DataTrigger>
            <DataTrigger Binding="{Binding IsChecked, ElementName=btnShowGameMenu}" Value="False">
                <Setter Property="Opacity" Value="0"/>
                <Setter Property="MaxHeight" Value="0"/>
                <DataTrigger.EnterActions>
                    <BeginStoryboard x:Name="GameMenuCollapse">
                        <Storyboard FillBehavior="Stop">
                            <DoubleAnimation Storyboard.TargetProperty="MaxHeight" From="36" To="0" Duration="0:0:0.5">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseInOut" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.5" />
                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Visibility">
                                <DiscreteObjectKeyFrame KeyTime="0:0:0.48" Value="{x:Static Visibility.Collapsed}" />
                            </ObjectAnimationUsingKeyFrames>
                        </Storyboard>
                    </BeginStoryboard>
                </DataTrigger.EnterActions>
                <DataTrigger.ExitActions>
                    <RemoveStoryboard BeginStoryboardName="GameMenuCollapse" />
                </DataTrigger.ExitActions>
            </DataTrigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="pmBorderStyle" TargetType="{x:Type Border}">
        <Setter Property="BorderBrush" Value="#FF521212" />
        <Setter Property="Background" Value="#B2393939" />
        <Setter Property="BorderThickness" Value="4,4,4,4" />
        <Setter Property="CornerRadius" Value="8,8,8,8" />
        <Setter Property="ClipToBounds" Value="True" />
        <Setter Property="Height" Value="148" />
        <Setter Property="Width" Value="265" />
    </Style>
</ResourceDictionary>
